(ns sp.styles.config
  (:require [garden.units :refer [percent vw em px]]
            [garden.color :refer [rgb rgba]]))

(def air (em 0.5))
(def header-font-weight 600)
(def type-scale (em 1.5))
(def red (rgb 212 12 50))
